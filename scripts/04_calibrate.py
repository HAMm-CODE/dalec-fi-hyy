#!/usr/bin/env python
"""Real-data NUTS calibration over the calibration block.

**Phase 8. Not yet implemented.**

Assimilates daily NEE_VUT_REF only, with a Gaussian likelihood whose per-day sd
is NEE_VUT_REF_RANDUNC. Writes the trace as NetCDF (never pickle) with the seed
recorded, plus the convergence summary.
"""

raise SystemExit("Phase 8 has not been built yet.")
